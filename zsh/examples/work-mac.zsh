# local.zsh — Work Mac overrides
# Copy to ~/.config/zsh/local.zsh and fill in your values.

# Company/project paths
# export PLAY_PATH="/path/to/your/project"
# export PATH="$PATH:$PLAY_PATH"

# API tokens (NEVER commit real values)
# export JIRA_API_TOKEN="your-token-here"
